# We have a conf and classes directory, add to BBPATH
BBPATH .= ":${LAYERDIR}"

# We have recipes-* directories, add to BBFILES
BBFILES += "${LAYERDIR}/recipes-*/*/*.bb \
            ${LAYERDIR}/recipes-*/*/*.bbappend"

BBFILE_COLLECTIONS += "meta-electra-display"
BBFILE_PATTERN_meta-electra-display = "^${LAYERDIR}/"
BBFILE_PRIORITY_meta-electra-display = "6"

LAYERDEPENDS_meta-electra-display = "core"
LAYERSERIES_COMPAT_meta-electra-display = "scarthgap"
